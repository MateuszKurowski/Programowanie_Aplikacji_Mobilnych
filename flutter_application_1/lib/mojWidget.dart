import 'package:flutter/material.dart';

class MojWidget extends StatelessWidget {
  const MojWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("To jest moj widget.");
  }
}
